class <%= @module_name %>.ApplicationDelegate extends CB.ApplicationDelegate
  applicationDidFinishLaunchingWithOptions: (options) ->
    CB.Log('App launch fine!')
    controller = new <%= @module_name %>.SampleViewController()
    CB.Window.currentWindow().rootViewController = controller
    return
