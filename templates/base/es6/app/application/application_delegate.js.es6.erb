<%= @module_name %>.ApplicationDelegate = class extends CB.ApplicationDelegate {
  applicationDidFinishLaunchingWithOptions(options) {
    var controller = new <%= @module_name %>.HelloViewController();
    CB.Window.currentWindow().rootViewController = controller;
  }
}
